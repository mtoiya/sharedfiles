# name: class ... { ... }
# key: class
# --
class ${1:Name}
{
public:
	${1:$(yas-substr yas-text "[^: ]*")}($2);
	virtual ~${1:$(yas-substr yas-text "[^: ]*")}();

private:
};

${1:$(yas-substr yas-text "[^: ]*")}::${1:$(yas-substr yas-text "[^: ]*")}(${2:$(yas-substr yas-text "[^: ]*")})
{
}

${1:$(yas-substr yas-text "[^: ]*")}::~${1:$(yas-substr yas-text "[^: ]*")}()
{
}
